{
  "name": "profile-rights-tool",
  "version": "0.1.0",
  "dependencies": {
    "getcomposer": "*",
    "material-components-web": "^0.16.0"
  },
  "scripts": {
    "live-setup": "npm --silent update --only=production && composer self-update && composer update --no-dev"
  }
}
